---
"@dualmark/core": minor
---

Consolidate path utilities and analytics types; trim the public API surface. Internal refactor — exported primitives (`detectAIBot`, `negotiateFormat`, `buildMarkdownResponse`, `renderLlmsTxt`, etc.) are unchanged.
